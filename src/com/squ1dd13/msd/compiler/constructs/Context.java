package com.squ1dd13.msd.compiler.constructs;

import java.util.*;

// Provides context for Compilable objects.
public class Context {
    // Streamed scripts (from script.img) have padded sizes.
    public boolean compilingStreamedScript = true;
}
