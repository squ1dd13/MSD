This package ("unified") is designed to replace the old compiler/decompiler structure with a single set of
script components that can be parsed from textual code, loaded from compiled files, and written to compiled files.
The aim is to reduce redundant code and make the codebase smaller.
